[build]
  command = "corepack enable && corepack prepare pnpm@10 --activate && pnpm install && PORT=3000 BASE_PATH=/ pnpm --filter @workspace/jivika-portfolio run build"
  publish = "artifacts/jivika-portfolio/dist/public"

[build.environment]
  NODE_VERSION = "24"
